# T09: Preserve funded records when guest access expires

> *This was generated by AI during triage.*

<!-- visibible-modernization:T09 -->

## Parent

[S02: Verified guest access and protected credit ownership](https://github.com/AustinKelsay/visibible/issues/46)

## What to build

Expired access cannot cause automated cleanup to erase purchased credit ownership.

## Acceptance criteria

- [ ] Preserve current idle/absolute access timeouts and reject expired credentials.
- [ ] Retain positive balances, financial links and unresolved reservations/invoices independently of access expiry.
- [ ] Eligible zero-balance cleanup checks linked state and uses bounded dry-run batches.
- [ ] Purchase and expiry notices explain that retained funds do not imply lost-cookie or cross-device recovery.
- [ ] Keep valid legacy session mappings and balances intact.

## Verification

Synthetic expired sessions with positive/zero balances, pending invoices, holds and finalized purchases; verify access rejection separately from record retention.

Run repository-required lint, type checks and relevant tests. For visible changes, verify the complete affected browser flow and attach screenshots. Update current behavior documentation with the implementation. No live paid evaluation or production migration is authorized by ticket publication; follow the applicable release procedure when implementing.

## Scope boundaries

No recovery feature and no attempt to authenticate an expired cookie.

## Blocked by

None (can start immediately).

## Traceability

Original backlog items: 28, 29. Baseline: `6ae456839208`. Read the parent spec for shared policy and domain decisions. `ready-for-agent` means specified; start only after blockers are complete.
