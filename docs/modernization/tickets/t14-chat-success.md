# T14: Release credits for embedded chat stream errors

> *This was generated by AI during triage.*

<!-- visibible-modernization:T14 -->

## Parent

[S04: Reliable Scripture chat with correct stream settlement](https://github.com/AustinKelsay/visibible/issues/48)

## What to build

A failed chat stream releases its hold even if the SDK closes the response normally.

## Acceptance criteria

- [ ] Use explicit stream outcome with accepted nonempty stop/length success; reject empty/error output as successful billing.
- [ ] Handle embedded error parts and thrown stream errors through one terminal settlement.
- [ ] Emit correct charged/refunded metadata and label length-limited output.
- [ ] Keep current chat persona and default model unchanged.

## Verification

Real installed SDK with fake model outputs: successful text, type:error followed by normal close, empty finish and thrown error; assert one charge or release through actual settlement interface.

Run repository-required lint, type checks and relevant tests. For visible changes, verify the complete affected browser flow and attach screenshots. Update current behavior documentation with the implementation. No live paid evaluation or production migration is authorized by ticket publication; follow the applicable release procedure when implementing.

## Scope boundaries

Immediate stream correctness fix; durable receipt integration follows independently.

## Blocked by

None (can start immediately).

## Traceability

Original backlog items: 17, 68. Baseline: `6ae456839208`. Read the parent spec for shared policy and domain decisions. `ready-for-agent` means specified; start only after blockers are complete.
