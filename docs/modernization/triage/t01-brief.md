> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T01 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Capture existing behavior and expose coverage gaps

**Current behavior:**
The audit maps current features and exposes coverage omissions, but a maintained acceptance inventory, coverage expansion and rendered regression example are not complete.

**Desired behavior:**
A maintainer can run an acceptance inventory and see which production modules are covered before replacing behavior.

**Key interfaces:**
- Actual Convex private/financial handlers, browser behavior and coverage reporting.
- Persisted executor/policy versions, migration/restore fixtures and public compatibility contracts.

**Acceptance criteria:**
- [ ] Record current reader/gallery/deep-link, translation, free-browse, credit, payment, API and Nostr contracts; identify accepted defects separately from behavior to preserve.
- [ ] Include relevant production implementations in coverage; publish the newly visible baseline and preserve existing thresholds through explicit scoped ratchets rather than claiming the old percentage covers all files.
- [ ] Replace at least one high-value source-string assertion with an equivalent rendered behavior check to establish reusable prior art.
- [ ] Keep this slice limited to inventory, coverage configuration and the example behavior test; do not rewrite application flows.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Run routine repo checks and prove an unimported central module appears in the coverage report. Demonstrate the example behavior regression fails when the behavior is intentionally broken.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Out of scope:**
- No blanket dependency on this ticket for urgent correctness fixes; later migration verification consumes its inventory.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S12: Verified migration and retirement of old implementations](https://github.com/AustinKelsay/visibible/issues/56). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 71, 72, 81.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
