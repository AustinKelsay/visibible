> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T02 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Correct chat and planner price units

**Current behavior:**
Chat and planner pricing helpers divide raw per-token catalog rates by one million again; fallback values and raw provider units do not share one reliable representation.

**Desired behavior:**
A chat or planner quote uses correct provider units in normal and catalog-outage conditions.

**Key interfaces:**
- Chat quote helpers (`estimateCost`, `computeChatCreditsCost`) and image quote/capability adapters.
- Wallet reserve/settle/release handlers and Neutral Cost/outbox reporting.

**Acceptance criteria:**
- [ ] Normalize live and emergency prices to one explicit per-token representation and remove duplicate per-million conversion.
- [ ] Use a shared validated monetary calculation for chat/planner quotes and metadata; keep one-credit minimum and 25% markup.
- [ ] Reject negative/nonfinite/malformed rates; distinguish known free usage from missing information.
- [ ] A fixture with USD 0.00001 per token and 1000 input plus 1000 output produces USD 0.02 and three credits; fallback mode uses the same units.
- [ ] Do not reprice historical charges or alter the default chat model.

**Verification:**
Runtime reproduced with real pricing helpers: 1,000 input and 1,000 output tokens at USD 0.00001/token return USD 0.00000002 and one credit; the provider-basis amount is USD 0.02.

Realistic catalog fixtures through quote and chat request paths, including free/missing prices, very small costs, outage fallback and overflow cases.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Out of scope:**
- Immediate correctness repair on current execution; durable receipts and new chat max-charge UI follow separately.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S01: Accurate quotes and auditable credit charges](https://github.com/AustinKelsay/visibible/issues/45). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 1, 2, 4, 5, 14, 67.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
