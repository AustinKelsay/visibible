> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T03 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Quote supported image settings from verified billing units

**Current behavior:**
Image discovery maps the raw input-image price to imageOutput; the quote has local resolution multipliers and multiplies already-rounded credits by 35 for holds.

**Desired behavior:**
A guest sees only supported image settings with prices derived from the correct output billing basis.

**Key interfaces:**
- Chat quote helpers (`estimateCost`, `computeChatCreditsCost`) and image quote/capability adapters.
- Wallet reserve/settle/release handlers and Neutral Cost/outbox reporting.

**Acceptance criteria:**
- [ ] Introduce a catalog/provider adapter that records explicit input/output units and capability provenance.
- [ ] Remove use of input-image price as a flat output price; verify output token/per-image/resolution fields for each enabled model.
- [ ] Apply the same normalized quote to selector and request validation; mark unavailable or unpriceable choices and explain fallback state.
- [ ] Retain supported current default behavior; adopting another image API is conditional on fixture/capability parity.
- [ ] Validate saved preferences and never inject a missing default as live-available solely because it is configured.

**Verification:**
The 35-fold hold and zero-versus-missing treatment were runtime characterized. The raw input-image-to-output-price mapping was independently confirmed in source; no paid model call was made.

Catalog fixtures with absent input-image price, explicit output units, unsupported resolution, stale catalog, default disappearance and malformed rates; assert UI quote equals server quote for same inputs.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Out of scope:**
- No default-model quality change. Versioned quote acceptance and reservation policy are a separate slice.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S01: Accurate quotes and auditable credit charges](https://github.com/AustinKelsay/visibible/issues/45). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 3, 7, 48, 49, 67.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
