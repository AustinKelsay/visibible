> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T04 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Show and enforce a maximum charge for paid work

**Current behavior:**
The existing wallet reserves and reconciles credits, but admissions do not expose a versioned customer maximum with matching fingerprint and expiry policy.

**Desired behavior:**
A guest accepts a versioned estimate and maximum charge, then sees a reservation that matches that authorization.

**Key interfaces:**
- Chat quote helpers (`estimateCost`, `computeChatCreditsCost`) and image quote/capability adapters.
- Wallet reserve/settle/release handlers and Neutral Cost/outbox reporting.

**Acceptance criteria:**
- [ ] Persist quote fingerprint/version and five-minute expiry; reject stale or mismatched acceptance before provider work.
- [ ] Show estimate and maximum in single-generation controls and chat admission; reserve the accepted maximum once.
- [ ] Use capped actual-usage settlement for new-policy operations; missing usage uses the accepted estimate and remains labeled estimated.
- [ ] Preserve positive-balance five-credit explicit-image grace with hold/max capped to balance; automatic generation requires full estimated affordability.
- [ ] Enforce actual input/output bounds and separate provider exposure from customer billing, including concurrent requests and daily reset.

**Verification:**
Runtime characterized current rounding/hold behavior: USD 0.001 produces a one-credit estimate and 35-credit reservation. The new maximum-charge policy remains unimplemented.

Quote -> accept -> reserve -> finish/release tests through existing entry points; test expiry, tampering, grace, changed catalog, cap breach and midnight release.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T02: Correct chat and planner price units](https://github.com/AustinKelsay/visibible/issues/58)
- [T03: Quote supported image settings from verified billing units](https://github.com/AustinKelsay/visibible/issues/59)

**Out of scope:**
- Keep existing operation policy version for in-flight work; no automatic top-up debit or historical repricing.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S01: Accurate quotes and auditable credit charges](https://github.com/AustinKelsay/visibible/issues/45). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 4, 6, 8, 10, 11.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
