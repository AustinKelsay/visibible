> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T05 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Use fresh cost samples and explicit estimate backfill

**Current behavior:**
Learned estimates already use the latest 30 samples and a 75th percentile, but selection lacks the requested freshness/minimum-sample gates and model browsing can backfill.

**Desired behavior:**
Guests receive estimates with defensible sample provenance; operators backfill estimates explicitly.

**Key interfaces:**
- Chat quote helpers (`estimateCost`, `computeChatCreditsCost`) and image quote/capability adapters.
- Wallet reserve/settle/release handlers and Neutral Cost/outbox reporting.

**Acceptance criteria:**
- [ ] Use at most 30 recent actual samples, a five-sample minimum and a 30-day freshness limit with model/settings normalization.
- [ ] Return source, sample count, age and confidence with learned estimates; fallback estimates never imply a guaranteed provider maximum.
- [ ] Remove backfill writes from model-choice GET handling.
- [ ] Provide a bounded resumable backfill with dry run, deterministic ordering and repeat-safe sample identity.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Test aged samples, sparse model/provider/global buckets, changed resolution support, duplicated history and repeated backfill; model browsing must perform no backfill mutation.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T03: Quote supported image settings from verified billing units](https://github.com/AustinKelsay/visibible/issues/59)

**Out of scope:**
- No new statistical model beyond the specified recent percentile approach.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S01: Accurate quotes and auditable credit charges](https://github.com/AustinKelsay/visibible/issues/45). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 12, 13.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
