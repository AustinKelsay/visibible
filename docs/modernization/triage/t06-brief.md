> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T06 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Finalize credits and durable cost reporting together

**Current behavior:**
A cost outbox already exists, but its intent is not atomic with settlement. Provider spend, planner spend and customer billing are not represented by one complete receipt.

**Desired behavior:**
An operator can recover one complete settlement receipt and cost projection after a crash.

**Key interfaces:**
- Chat quote helpers (`estimateCost`, `computeChatCreditsCost`) and image quote/capability adapters.
- Wallet reserve/settle/release handlers and Neutral Cost/outbox reporting.

**Acceptance criteria:**
- [ ] Atomically persist wallet effect, immutable receipt and outbox intent keyed to the operation.
- [ ] Store provider spend, planner spend, estimate, hold, charged credits, released credits and absorbed shortfall separately with usage provenance.
- [ ] Project to Neutral Cost through atomic deduplication; replay and concurrent workers cannot create duplicate events.
- [ ] Expose pending/failed delivery with bounded retries; known zero is not missing usage.
- [ ] Record provider corrections separately without reopening settled guest charges.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Crash after settlement before delivery, double processing, provider success with unusable plan, missing usage and projection outage against real settlement/delivery handlers.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T04: Show and enforce a maximum charge for paid work](https://github.com/AustinKelsay/visibible/issues/60)

**Out of scope:**
- Neutral Cost remains a reporting projection. Do not create a second authoritative wallet.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S01: Accurate quotes and auditable credit charges](https://github.com/AustinKelsay/visibible/issues/45). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 8, 9, 15, 16.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
