> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T07 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Authenticate anonymous Convex access from valid sessions

**Current behavior:**
The browser uses an unauthenticated ConvexProvider. Existing signed HTTP guest cookies are useful but do not establish verified identity inside private Convex handlers.

**Desired behavior:**
An existing guest opens the app and receives verified Convex identity without registering or moving their balance.

**Key interfaces:**
- Verified guest token broker and Convex identity boundary, preserving the existing SID mapping.
- `getSession`, `getCreditHistory`, invoice reads, bulk ownership checks and SessionProvider.

**Acceptance criteria:**
- [ ] Issue short-lived RS256 tokens from valid signed HttpOnly sessions with verified issuer, audience, kid, iat and expiry.
- [ ] Map subject to existing SID; derive tier from current server records; keep tokens in browser memory.
- [ ] Connect client token refresh and key rotation; enforce underlying session expiry and revocation.
- [ ] Protect issuance by origin/CSRF/rate checks and expose only public verification keys.
- [ ] Free library browsing works without a valid private identity.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Test wrong signature/issuer/audience, expiry, rotated keys, revoked session, stale admin tier and successful existing-session balance lookup.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Out of scope:**
- No accounts, cross-device recovery or relaxed cookie expiry. This adds verified identity before migrating private callers.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S02: Verified guest access and protected credit ownership](https://github.com/AustinKelsay/visibible/issues/46). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 25.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
