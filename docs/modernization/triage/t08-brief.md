> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T08 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Enforce ownership on private records and job controls

**Current behavior:**
Private session/invoice queries and bulk ownership checks accept identifiers without verified subject ownership. Protected paid HTTP routes and server-secret writes already exist and must be preserved.

**Desired behavior:**
Guests can access their own credit, invoice and bulk records while foreign identifiers grant no access.

**Key interfaces:**
- Verified guest token broker and Convex identity boundary, preserving the existing SID mapping.
- `getSession`, `getCreditHistory`, invoice reads, bulk ownership checks and SessionProvider.

**Acceptance criteria:**
- [ ] Inventory every exported Convex function and classify it public, guest-owned or internal/server-only.
- [ ] Migrate private reads and controls to server-derived subject ownership; reject caller SID as proof.
- [ ] Preserve server-side payment/generation callers through scoped authenticated interfaces.
- [ ] Keep public image API projections unchanged; exclude private session and invoice data.
- [ ] Check job mutation ownership and expiry through the same identity rule.

**Verification:**
Runtime reproduced against the real getSession handler with fake database state and an unauthenticated identity: a known foreign SID returned its balance without consulting identity. This does not demonstrate arbitrary paid-route balance theft.

Two guests plus unauthenticated client call actual handlers with known foreign IDs; verify denial for balance, history, invoice and bulk controls, and continued public browsing.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T07: Authenticate anonymous Convex access from valid sessions](https://github.com/AustinKelsay/visibible/issues/63)

**Out of scope:**
- Bulk outcome/counter writes become worker-only in the bulk-execution ticket; this slice closes identity gaps first.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S02: Verified guest access and protected credit ownership](https://github.com/AustinKelsay/visibible/issues/46). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 26, 27.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
