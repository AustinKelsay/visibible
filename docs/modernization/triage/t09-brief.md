> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T09 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Preserve funded records when guest access expires

**Current behavior:**
The cleanup mutation deletes expired session rows without checking purchased balance or linked financial state. Access expiry and financial record retention are conflated.

**Desired behavior:**
Expired access cannot cause automated cleanup to erase purchased credit ownership.

**Key interfaces:**
- Verified guest token broker and Convex identity boundary, preserving the existing SID mapping.
- `getSession`, `getCreditHistory`, invoice reads, bulk ownership checks and SessionProvider.

**Acceptance criteria:**
- [ ] Preserve current idle/absolute access timeouts and reject expired credentials.
- [ ] Retain positive balances, financial links and unresolved reservations/invoices independently of access expiry.
- [ ] Eligible zero-balance cleanup checks linked state and uses bounded dry-run batches.
- [ ] Purchase and expiry notices explain that retained funds do not imply lost-cookie or cross-device recovery.
- [ ] Keep valid legacy session mappings and balances intact.

**Verification:**
Runtime reproduced against the real cleanup handler and fake state: an expired row with 100 credits was passed to deletion. No live record was deleted.

Synthetic expired sessions with positive/zero balances, pending invoices, holds and finalized purchases; verify access rejection separately from record retention.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Dependency correction:**
Funded-record retention can be fixed using existing session records and current cookie expiry checks. Issuing Convex JWTs is not a prerequisite; no new account recovery behavior is added.

**Out of scope:**
- No recovery feature and no attempt to authenticate an expired cookie.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S02: Verified guest access and protected credit ownership](https://github.com/AustinKelsay/visibible/issues/46). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 28, 29.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
