> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T11 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Credit settled invoices first observed after expiry

**Current behavior:**
The invoice routes and confirmation handler reject local expiry before authoritative late-observed settlement can grant credits. Existing paid-invoice checks prevent some duplicate confirmation.

**Desired behavior:**
A guest who paid on time receives credits even when the first successful status check is late.

**Key interfaces:**
- `createLndInvoice`, `lookupLndInvoice`, application invoice identity and creation intent.
- `confirmPaymentInternal`, purchase ledger entries and scheduled reconciliation.

**Acceptance criteria:**
- [ ] Read and validate LND settlement before using local expiry to reject credit confirmation.
- [ ] Apply the rule consistently to status GET, explicit confirmation and internal finalization.
- [ ] Make invoice-paid and purchase credit addition atomic and uniquely keyed.
- [ ] Distinguish provider outage from unpaid/expired state; never grant credits from an unverified client report.
- [ ] Keep existing bundles and paid/admin tier behavior.

**Verification:**
Runtime reproduced the real confirmation handler rejecting a pending invoice whose local deadline elapsed. The fake database does not model transaction rollback, and this does not establish an actual customer incident.

Actual invoice/credit handlers with fake LND: settled before deadline but observed late, unpaid expired, outage, insufficient paid amount and concurrent duplicate confirmation.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Out of scope:**
- Independent hotfix; do not wait for the background worker or auth migration.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S03: Lightning purchases that settle without an open tab](https://github.com/AustinKelsay/visibible/issues/47). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 21, 24.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
