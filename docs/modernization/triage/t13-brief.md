> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T13 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Reconcile Lightning invoices without browser polling

**Current behavior:**
Invoice observation is driven by HTTP/browser checks. Existing cron work does not include durable reconciliation of pending or locally expired invoices.

**Desired behavior:**
A completed payment updates the guest balance even while every browser tab is closed.

**Key interfaces:**
- `createLndInvoice`, `lookupLndInvoice`, application invoice identity and creation intent.
- `confirmPaymentInternal`, purchase ledger entries and scheduled reconciliation.

**Acceptance criteria:**
- [ ] Schedule bounded server reconciliation from recorded invoice intents; validate execution-environment LND reachability.
- [ ] Use direct server adapter or a narrowly authenticated bridge if required; no browser-driven settlement dependency.
- [ ] Apply pending polling/backoff and historical locally-expired sweep from S03; expose unresolved age and retry status.
- [ ] Reuse atomic confirmation and never issue a second purchase credit.
- [ ] Keep invoice expiry display separate from verified settled truth.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Close browser, settle fake LND invoice, advance scheduler, reopen; test outages, restart, backoff and late historical settlement.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T12: Recover interrupted invoice creation](https://github.com/AustinKelsay/visibible/issues/68)

**Out of scope:**
- No broader LND privileges, real-money test requirement or public payment callback without authentication.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S03: Lightning purchases that settle without an open tab](https://github.com/AustinKelsay/visibible/issues/47). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 22.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
