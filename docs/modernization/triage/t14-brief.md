> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T14 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Release credits for embedded chat stream errors

**Current behavior:**
The credit deduction transform charges in flush. The installed SDK can emit an error as normal stream data and still close normally, reaching that branch.

**Desired behavior:**
A failed chat stream releases its hold even if the SDK closes the response normally.

**Key interfaces:**
- Chat admission, validated message roles and server-owned passage context.
- `streamText` outcomes, response metadata, abort propagation and `settleCredits`.

**Acceptance criteria:**
- [ ] Use explicit stream outcome with accepted nonempty stop/length success; reject empty/error output as successful billing.
- [ ] Handle embedded error parts and thrown stream errors through one terminal settlement.
- [ ] Emit correct charged/refunded metadata and label length-limited output.
- [ ] Keep current chat persona and default model unchanged.

**Verification:**
Runtime reproduced with the installed AI SDK and a fake provider: an embedded error arrived as UI stream data, then the transform flush ran normally. Source review connects that flush to the current charge branch.

Real installed SDK with fake model outputs: successful text, type:error followed by normal close, empty finish and thrown error; assert one charge or release through actual settlement interface.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Dependency correction:**
Stream success/error settlement can be corrected against the current wallet and SDK independently of pricing arithmetic. Preserve the existing pricing interface until its separate repair lands.

**Out of scope:**
- Immediate stream correctness fix; durable receipt integration follows independently.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S04: Reliable Scripture chat with correct stream settlement](https://github.com/AustinKelsay/visibible/issues/48). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 17, 68.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
