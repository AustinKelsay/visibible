> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T15 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Cancel chat safely through the provider and wallet

**Current behavior:**
The chat request does not pass its abort signal into streamText. Stream cancellation and settlement span multiple callbacks rather than one explicit outcome.

**Desired behavior:**
Cancelling chat stops processing and settles the reservation once.

**Key interfaces:**
- Chat admission, validated message roles and server-owned passage context.
- `streamText` outcomes, response metadata, abort propagation and `settleCredits`.

**Acceptance criteria:**
- [ ] Connect request cancellation to provider abort and the active stream reader.
- [ ] Resolve cancellation racing success according to the first recorded terminal outcome.
- [ ] Release holds for cancelled/error streams and retain known provider spend.
- [ ] Make disconnect/abort cleanup safe to repeat without hiding a failed release.

**Verification:**
The streamText call omits the incoming abort signal. Provider cancellation and settlement races require a fake-provider regression before changing behavior.

Real SDK fake provider with cancellation before tokens, midstream and after success; assert cancellation signal and single final wallet effect.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T14: Release credits for embedded chat stream errors](https://github.com/AustinKelsay/visibible/issues/70)

**Out of scope:**
- Provider-side cancellation is best-effort; do not claim it erases already incurred spend.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S04: Reliable Scripture chat with correct stream settlement](https://github.com/AustinKelsay/visibible/issues/48). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 18.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
