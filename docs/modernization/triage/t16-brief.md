> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T16 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Resolve canonical passages and preserve unavailable locations

**Current behavior:**
The reader redirects some failed or missing Scripture lookups to Genesis 1:1, losing the intended reference. Reference/count data and translation coverage require consistent handling.

**Desired behavior:**
A reader stays at the intended passage when text is unavailable and gets correct adjacent navigation.

**Key interfaces:**
- `getVerse`, `getChapter`, `getVerseByReference` and canonical navigation rules.
- `getScenePlanCache`, planner input provenance and prompt-version compatibility.

**Acceptance criteria:**
- [ ] Consolidate book identity and reference/adjacency rules behind the existing Scripture interface.
- [ ] Distinguish invalid location, missing translation coverage and retryable upstream failure.
- [ ] Render retry/translation alternatives at the requested URL instead of redirecting failures to Genesis.
- [ ] Verify all selectable translations against representative missing-coverage and book-edge fixtures; document limitations.
- [ ] Preserve existing canonical URLs, scope semantics and first/last verse behavior.

**Verification:**
The canonical reader failure/null branches redirect to Genesis 1:1. A rendered failing-lookup regression remains part of implementation verification.

Scripture resolve/navigation tests and rendered error states for upstream timeout, missing translation, invalid reference and cross-book edges.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Out of scope:**
- No new corpus or translation. Replace duplicate metadata only where needed to keep one reference authority.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S07: Canonical Scripture and versioned scene plans](https://github.com/AustinKelsay/visibible/issues/51). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 62, 64, 65.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
