> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T17 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Bound chapter caching and concurrent lookup work

**Current behavior:**
The Scripture adapter already caches chapters by book/chapter/translation in a process-local Map, without bounded eviction, TTL or in-flight request coalescing.

**Desired behavior:**
Repeated reading uses a predictable chapter cache without unlimited process growth or duplicate in-flight fetches.

**Key interfaces:**
- `getVerse`, `getChapter`, `getVerseByReference` and canonical navigation rules.
- `getScenePlanCache`, planner input provenance and prompt-version compatibility.

**Acceptance criteria:**
- [ ] Prefer the existing platform cache; retain a process layer only with demonstrated need.
- [ ] If retained, enforce 256-entry maximum and 24-hour TTL, with in-flight request deduplication.
- [ ] Bound external waits and represent retryable failures without caching them as valid empty chapters.
- [ ] Keep translation in cache identity and preserve existing successful passage output.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Fake clock/provider tests for eviction, expiry, concurrent same-chapter calls, translation isolation and recovery after failure.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T16: Resolve canonical passages and preserve unavailable locations](https://github.com/AustinKelsay/visibible/issues/72)

**Out of scope:**
- No framework upgrade or new storage system required.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S07: Canonical Scripture and versioned scene plans](https://github.com/AustinKelsay/visibible/issues/51). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 63.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
