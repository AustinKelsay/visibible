> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T19 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Reuse only compatible scene plans

**Current behavior:**
The scene cache stores planner/prompt metadata, but its lookup only keys on verse, translation and style. An incompatible stored plan can be returned.

**Desired behavior:**
A guest gets a cached plan only when canonical inputs and prompt/model/style versions match.

**Key interfaces:**
- `getVerse`, `getChapter`, `getVerseByReference` and canonical navigation rules.
- `getScenePlanCache`, planner input provenance and prompt-version compatibility.

**Acceptance criteria:**
- [ ] Extract bounded prompt construction and plan normalization behind the generation module.
- [ ] Key cache by passage fingerprint, translation and relevant planner/prompt/style/theme/continuity versions.
- [ ] Separate cache query from optional hit accounting; old incompatible entries miss safely.
- [ ] Keep classical style, existing prompt limits and variation intent as the evaluation baseline.

**Verification:**
Runtime reproduced the real cache handler returning stored old prompt/planner metadata. Source review confirms the missing version gate; no live planner call was made.

Cache hit/miss behavior for each version/input change, malformed planner output and empty/oversized fields; compare bounded prompt output on existing fixtures.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T18: Build shared images from verified Scripture text](https://github.com/AustinKelsay/visibible/issues/74)

**Out of scope:**
- Any changed artistic output must also satisfy the model-quality release spec.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S07: Canonical Scripture and versioned scene plans](https://github.com/AustinKelsay/visibible/issues/51). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 46, 47.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
