> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T20 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Bound chat input and keep system instructions server-owned

**Current behavior:**
Chat bounds message count and some context strings, but accepts client system roles and client passage context and does not enforce the specified total input/output token budget.

**Desired behavior:**
A guest can ask about the canonical passage with bounded history and cannot submit privileged system instructions.

**Key interfaces:**
- Chat admission, validated message roles and server-owned passage context.
- `streamText` outcomes, response metadata, abort propagation and `settleCredits`.

**Acceptance criteria:**
- [ ] Accept explicit user/assistant history and supported parts; reject client system roles.
- [ ] Resolve passage context on the server and bound serialized history against model capacity.
- [ ] Enforce model-compatible output token limit with default maximum 2048; show rejection or length status clearly.
- [ ] Keep request admission, context assembly and stream outcome behind the chat interface while preserving output intent.
- [ ] Complete required baseline/candidate chat evaluation for context or provider behavior changes before release.

**Verification:**
The validated role enum includes system and it is preserved into modelMessages; streamText admission has no specified output cap. End-to-end hostile-role/token-budget regression remains required.

Actual chat interface tests for role injection, altered passage context, overlong history, unsupported parts and context-window edge; real SDK fakes for bounded output.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T16: Resolve canonical passages and preserve unavailable locations](https://github.com/AustinKelsay/visibible/issues/72)
- [T15: Cancel chat safely through the provider and wallet](https://github.com/AustinKelsay/visibible/issues/71)

**Out of scope:**
- No persistent conversations or new persona. Live evaluation is subject to the existing release gate.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S04: Reliable Scripture chat with correct stream settlement](https://github.com/AustinKelsay/visibible/issues/48). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 19, 20, 11.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
