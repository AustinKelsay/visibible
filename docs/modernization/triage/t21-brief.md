> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T21 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Attach durable quotes and receipts to chat turns

**Current behavior:**
Chat already reserves and settles credits, but it lacks the shared versioned quote/receipt interface proposed for auditable accounting.

**Desired behavior:**
A completed or failed chat turn has one durable receipt matching its displayed charge.

**Key interfaces:**
- Chat admission, validated message roles and server-owned passage context.
- `streamText` outcomes, response metadata, abort propagation and `settleCredits`.

**Acceptance criteria:**
- [ ] Accept the versioned chat quote before provider work and carry stable operation identity through streaming.
- [ ] Finalize actual-usage capped charge or release through the shared wallet/receipt interface.
- [ ] Record provider costs for failures and label estimated cost when usage is absent.
- [ ] Return receipt identity and final charge in metadata; no duplicate billing on terminal callbacks.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Stream success/error/cancel with changing catalog, missing usage, projection failure and duplicate callbacks; compare UI metadata with immutable receipt.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T20: Bound chat input and keep system instructions server-owned](https://github.com/AustinKelsay/visibible/issues/76)
- [T06: Finalize credits and durable cost reporting together](https://github.com/AustinKelsay/visibible/issues/62)

**Out of scope:**
- No persisted message content required; retain only the usage and outcome metadata needed for this contract.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S04: Reliable Scripture chat with correct stream settlement](https://github.com/AustinKelsay/visibible/issues/48). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 8, 10, 20.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
