> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T22 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Deduplicate image requests and guard terminal states

**Current behavior:**
Each image HTTP attempt receives a fresh billing ID; the existing request-creation duplicate result is ignored. Terminal request states can be overwritten by later progress.

**Desired behavior:**
Retrying the current image route reuses one generation, while changed inputs with the same ID are rejected.

**Key interfaces:**
- `createGenerationRequest`, `updateGenerationRequest` and authenticated HTTP intent admission.
- Generation attempts, durable workflow steps, saved-image identity and wallet finalization.

**Acceptance criteria:**
- [ ] Scope intent to verified HTTP guest identity and immutable canonical input fingerprint.
- [ ] Atomically create/claim the operation and reuse its billing identity across transport retries.
- [ ] Honor already-existing operations; return known progress/result and never blindly repeat the provider call.
- [ ] Enforce valid state transitions and immutable terminal outcomes.
- [ ] Record an executor/policy version on admission so a later migration can route the same operation safely.

**Verification:**
Runtime reproduced terminal succeeded -> generating regression in the real lifecycle handler. Source review separately confirms fresh billing identity per HTTP attempt and ignored duplicate-request result; concurrent paid POSTs were not run.

Concurrent identical POSTs, same ID with different owner/input, duplicate terminal updates and lost HTTP response; assert one provider attempt/reservation.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Dependency correction:**
Stable request and billing identity can be repaired on the current signed-cookie HTTP path. Persist the legacy policy/executor version; new maximum-charge quotes are not a prerequisite.

**Out of scope:**
- Hotfix current execution first; this ticket does not require the new Convex workflow.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S05: Durable single-verse image generation](https://github.com/AustinKelsay/visibible/issues/49). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 31, 32, 33.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
