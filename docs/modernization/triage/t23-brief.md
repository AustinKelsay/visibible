> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T23 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Save each generation once and retain its image identity

**Current behavior:**
Storage checks for existing generation output before a later insert. The final insertion mutation itself accepts duplicate generation identities, leaving a concurrent write gap.

**Desired behavior:**
Repeated image-save processing produces one durable image row and a stable lookup result.

**Key interfaces:**
- `saveImage`, `saveImageWithStorage`, generation uniqueness and saved-image lookup.
- `getImageHistory`, chapter gallery/status and existing public paginated projections.

**Acceptance criteria:**
- [ ] Move generation uniqueness check into the insertion mutation.
- [ ] Keep saved-image identity and provenance stable across retried actions.
- [ ] Separate storage persistence from unrelated library-query logic only as needed for a clear save/read interface.
- [ ] Detect orphaned blobs safely; any cleanup is bounded and verifies absence of references.

**Verification:**
Runtime reproduced two inserts with the same generation ID through the real final insertion handler and fake storage/database state. Source review confirms the preceding action-level lookup is outside that mutation.

Concurrent saves with same generation ID against actual insertion handlers; duplicate/retry returns same image, different generations remain distinct.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Out of scope:**
- No library pagination or deletion of legacy images.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S08: Reliable shared image storage and bounded library reads](https://github.com/AustinKelsay/visibible/issues/52). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 37, 52.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
