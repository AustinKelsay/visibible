> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T24 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Constrain remote image storage fetches

**Current behavior:**
Remote image fetching already checks initial host, type and size, but default redirect following and unresolved destination DNS leave the complete fetch policy unenforced.

**Desired behavior:**
Valid provider image output can be stored while forbidden destinations and malformed payloads are rejected.

**Key interfaces:**
- `saveImage`, `saveImageWithStorage`, generation uniqueness and saved-image lookup.
- `getImageHistory`, chapter gallery/status and existing public paginated projections.

**Acceptance criteria:**
- [ ] Prefer returned image bytes and preserve MIME, size and timeout enforcement through full body read.
- [ ] Reject redirects by default; any supported redirect revalidates the destination before fetching.
- [ ] Validate a runtime-compatible protection against private/DNS-rebound targets, or reject remote URL mode when that protection cannot be established.
- [ ] Return an explicit persistence error rather than creating a durable-success claim for rejected output.

**Verification:**
The remote fetch supplies a timeout signal but no redirect policy or per-hop destination validation. A controlled redirect/DNS fixture is required before claiming exploitability or a fix.

Fake network responses for redirect chains, private hosts, DNS changes where testable, oversize/chunked bodies, invalid MIME and delayed body completion.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Out of scope:**
- Do not expand allowlists or claim DNS protection based solely on string hostname checks.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S08: Reliable shared image storage and bounded library reads](https://github.com/AustinKelsay/visibible/issues/52). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 53.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
