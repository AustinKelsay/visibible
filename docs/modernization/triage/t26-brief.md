> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T26 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Recover provider ambiguity and image storage failures

**Current behavior:**
Provider attempts, storage retries and wallet outcomes do not form a recoverable durable operation; ambiguous external success cannot safely be treated as ordinary retryable failure.

**Desired behavior:**
An interrupted generation is reconciled from its recorded attempt rather than silently generating another paid image.

**Key interfaces:**
- `createGenerationRequest`, `updateGenerationRequest` and authenticated HTTP intent admission.
- Generation attempts, durable workflow steps, saved-image identity and wallet finalization.

**Acceptance criteria:**
- [ ] Record attempt identity before provider call and persist response ID/usage/output as soon as possible.
- [ ] Reconcile known provider outcomes using verified capabilities; unresolved attempts enter uncertain without automatic paid retry.
- [ ] Retry saving existing output without regeneration; expose actionable persistence/uncertain status.
- [ ] After the 30-minute unresolved customer hold limit, release once and retain reconciliation evidence; late cost discovery cannot reopen the guest debit.
- [ ] Retain dedupe identity after diagnostic data is archived.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Failure injection after provider success before local acknowledgement, after output save before finalization, lost storage response and delayed provider lookup.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T25: Complete one image through a durable Convex workflow](https://github.com/AustinKelsay/visibible/issues/81)

**Out of scope:**
- No exactly-once claim for external providers that lack verifiable idempotency.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S05: Durable single-verse image generation](https://github.com/AustinKelsay/visibible/issues/49). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 35, 38, 70.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
