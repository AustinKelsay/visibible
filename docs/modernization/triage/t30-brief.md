> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T30 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Recover interrupted bulk items from generation truth

**Current behavior:**
Browser recovery identifies an in-progress item but rebuilds executable work from queued rows. A generating item has no demonstrated server-owned outcome reconciliation path.

**Desired behavior:**
A restarted job resolves abandoned generating items and continues without repeating completed work.

**Key interfaces:**
- Bulk job/item create, claim, outcome, pause/resume/cancel interfaces.
- Shared single-image generation command, server scheduling and reactive job progress.

**Acceptance criteria:**
- [ ] Reconcile each abandoned claim against linked generation state/result.
- [ ] Finalize counters once for completed/failed items and preserve uncertain attempts for safe resolution.
- [ ] Prevent a stale worker from overwriting a newer or terminal item outcome.
- [ ] Expose recovery status instead of leaving a permanent generating spinner.

**Verification:**
Recovery filters the executable queue to queued rows while separately displaying generating state. Crash/reopen recovery has not been reproduced in a deployed browser; implementation must test it with fake provider effects.

Crash before generation, after saved image, after settlement and before parent counter update; restart and verify exact totals and no additional provider call.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T28: Run a small bulk job entirely on the server](https://github.com/AustinKelsay/visibible/issues/84)

**Out of scope:**
- No blind requeue of uncertain paid attempts.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S06: Bulk generation managed by the server](https://github.com/AustinKelsay/visibible/issues/50). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 41, 70.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
