> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T34 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Serve image counts and availability from bounded summaries

**Current behavior:**
Chapter/book availability reads traverse image records using indexed but unbounded collection. Small transactional summaries are not yet the shared query authority.

**Desired behavior:**
Book menus and verse strips show accurate image counts without reading all image history.

**Key interfaces:**
- `saveImage`, `saveImageWithStorage`, generation uniqueness and saved-image lookup.
- `getImageHistory`, chapter gallery/status and existing public paginated projections.

**Acceptance criteria:**
- [ ] Maintain latest-image and count summaries during image insert/delete using the same transaction where feasible.
- [ ] Backfill in bounded repeatable batches while preserving stable IDs.
- [ ] Compare summary output against reference queries before switching book/chapter/verse consumers.
- [ ] Measure representative query reads and return sizes for large histories.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Insert/delete/retry/backfill scenarios and reconciliation against source rows, plus menu/verse-strip behavior tests.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T23: Save each generation once and retain its image identity](https://github.com/AustinKelsay/visibible/issues/79)

**Out of scope:**
- No aggregation component required unless measurements justify it.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S08: Reliable shared image storage and bounded library reads](https://github.com/AustinKelsay/visibible/issues/52). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 51.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
