> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T35 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Bound publication impressions and measure reactive fan-out

**Current behavior:**
Impressions mutate image records used by reactive readers and can be repeatedly incremented without the proposed guest/window bound. Fan-out cost has not been measured in production.

**Desired behavior:**
Publication ranking counts repeat views under an explicit rule without avoidable gallery recomputation.

**Key interfaces:**
- `publishToNostr`, publication identities/window claims and impression recording.
- Durable operation views, metric/health snapshots and bounded cleanup policies.

**Acceptance criteria:**
- [ ] Count at most one eligible impression per authenticated guest/image/four-hour window and enforce a bounded write rate.
- [ ] Preserve public image browsing; unauthenticated views need not count toward ranking.
- [ ] Measure history/gallery invalidations at representative loads and move counters away from image records if needed.
- [ ] Document that guest uniqueness is an abuse control, not unique-human proof; keep ranking output inspectable.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Repeated/simultaneous impressions, multiple images/windows, foreign IDs and load comparison before/after; attach measurements even if no counter move is warranted.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T07: Authenticate anonymous Convex access from valid sessions](https://github.com/AustinKelsay/visibible/issues/63)
- [T33: Browse paginated history without breaking image links](https://github.com/AustinKelsay/visibible/issues/89)

**Out of scope:**
- No social voting or tracking of extra identifying data solely for perfect uniqueness.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S11: Trustworthy publication, operations and retention](https://github.com/AustinKelsay/visibible/issues/55). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 54, 55.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
