> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T36 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Keep Nostr publication identity stable through failures

**Current behavior:**
Nostr has publication checks and scheduled window claims, but signs/publishes before storing the resulting event identity. A record failure after relay acceptance leaves an ambiguous retry.

**Desired behavior:**
A scheduled image is shared without generating duplicate events when recording or relay acknowledgement fails.

**Key interfaces:**
- `publishToNostr`, publication identities/window claims and impression recording.
- Durable operation views, metric/health snapshots and bounded cleanup policies.

**Acceptance criteria:**
- [ ] Persist the signed event identity before external publication and retry only that same event when safe.
- [ ] Record relay outcomes and preserve historical publication IDs and conservative ambiguous-window handling.
- [ ] Keep latest completed four-hour selection and skip missed windows rather than automatically flooding a backlog.
- [ ] Provide operator-visible unresolved status with safe reconciliation steps.

**Verification:**
The signed event is sent before publication identity persistence. The lost-acknowledgement/record-write failure path is source established; no relay was contacted.

Fake relays: success then local failure, partial acknowledgements, timeout, restart and scheduler downtime; assert at most one signed event identity per selected publication.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
None (can start immediately).

**Out of scope:**
- No new relay platform or live publication during tests.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S11: Trustworthy publication, operations and retention](https://github.com/AustinKelsay/visibible/issues/55). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 56.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
