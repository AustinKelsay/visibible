> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T37 -->

## Agent Brief

**Category:** bug
**State:** ready-for-agent
**Summary:** Use one generation interface in reader gallery and header

**Current behavior:**
GenerationContext receives commands from HeroImage; gallery mode unmounts that renderer. Header controls and manual history refresh depend on renderer-owned state.

**Desired behavior:**
Guests can start and recover a generation from the available verse controls without requiring HeroImage to own the command.

**Key interfaces:**
- GenerationContext command registration and reader/gallery/header consumers.
- PreferencesProvider, welcome/purchase/admin states and authenticated balance/invoice queries.

**Acceptance criteria:**
- [ ] Move command/quote/status ownership out of the renderer into a verse generation interface.
- [ ] Connect reader, gallery and header controls to the same selected passage and operation.
- [ ] Keep visual image-selection/loading/fullscreen/gesture state local and separate from paid work.
- [ ] Remove manual history refresh tokens once subscriptions and direct selected-image lookup cover updates.
- [ ] Preserve auto-generation affordability and avoid duplicate effects when switching views.

**Verification:**
Generation commands are registered by the image renderer, which gallery view unmounts. The proposed cross-view behavior needs a rendered browser regression; no new visual verification is claimed.

Reader -> gallery -> generate -> navigate away -> reopen result; mount/unmount renderer; passive image load versus paid progress; verify no extra provider request.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T27: Resolve image cancellation against durable completion](https://github.com/AustinKelsay/visibible/issues/83)
- [T33: Browse paginated history without breaking image links](https://github.com/AustinKelsay/visibible/issues/89)

**Out of scope:**
- No visual redesign; use existing controls and layout.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S09: Consistent reader controls and preferences](https://github.com/AustinKelsay/visibible/issues/53). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 57, 58, 59.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
