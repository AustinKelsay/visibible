> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T38 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Make preference hydration and persistence consistent

**Current behavior:**
Preference values span cookies, localStorage and component state, with separate hydration/persistence paths. The proposed authority and migration rules are not yet consolidated.

**Desired behavior:**
A reader keeps valid translation/model/settings choices across refresh without conflicting storage copies.

**Key interfaces:**
- GenerationContext command registration and reader/gallery/header consumers.
- PreferencesProvider, welcome/purchase/admin states and authenticated balance/invoice queries.

**Acceptance criteria:**
- [ ] Use cookies as authority for server-visible translation/model preferences and versioned localStorage for client-only settings/boot hints.
- [ ] Migrate existing values and handle unavailable models/settings explicitly.
- [ ] Prevent hydration effects from reverting a user choice or triggering paid work.
- [ ] Preserve route refresh semantics only where server-visible content actually changes.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Stale cookie/localStorage combinations, fresh browser, unavailable model, translation switch and rapid selection during hydration; verify rendered choices and next request settings.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T03: Quote supported image settings from verified billing units](https://github.com/AustinKelsay/visibible/issues/59)

**Out of scope:**
- No cross-device preference sync.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S09: Consistent reader controls and preferences](https://github.com/AustinKelsay/visibible/issues/53). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 60.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
