> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T40 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Protect reader interaction through behavior tests

**Current behavior:**
Some rendered interaction tests already exist alongside source-string checks. Complete coverage of the replacement generation/preferences flows cannot be established before those flows exist.

**Desired behavior:**
A maintainer can refactor reader internals while tests protect the interactions readers actually use.

**Key interfaces:**
- GenerationContext command registration and reader/gallery/header consumers.
- PreferencesProvider, welcome/purchase/admin states and authenticated balance/invoice queries.

**Acceptance criteria:**
- [ ] Cover keyboard/touch navigation, Escape order, focus restoration and fullscreen across reader/gallery.
- [ ] Protect deep links, image selection and passive loading/error behavior on narrow/wide layouts.
- [ ] Replace corresponding source-text assertions only after behavior coverage exists.
- [ ] Record visual checks and fix regressions introduced by replacement, keeping established design.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Browser acceptance suite with deterministic Scripture/image fixtures and no paid provider calls; demonstrate failures for broken interaction contracts.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T37: Use one generation interface in reader gallery and header](https://github.com/AustinKelsay/visibible/issues/93)
- [T38: Make preference hydration and persistence consistent](https://github.com/AustinKelsay/visibible/issues/94)

**Out of scope:**
- Test slice limited to reader interactions, not broad snapshot coverage of every component.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S09: Consistent reader controls and preferences](https://github.com/AustinKelsay/visibible/issues/53). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 71, 81.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
