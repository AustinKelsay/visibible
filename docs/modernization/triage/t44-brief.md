> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T44 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Exercise private and financial Convex handlers directly

**Current behavior:**
Tests already exercise some real session handlers with fake state, while key invoice/job/image/cost implementations are often omitted or replaced by handwritten mocks.

**Desired behavior:**
The core ownership and financial tests use real handlers instead of a second handwritten billing implementation.

**Key interfaces:**
- Actual Convex private/financial handlers, browser behavior and coverage reporting.
- Persisted executor/policy versions, migration/restore fixtures and public compatibility contracts.

**Acceptance criteria:**
- [ ] Cover session ownership, invoice confirmation, quote reservation, settlement receipt and outbox invariants using actual handlers.
- [ ] Replace business-rule duplication in route mocks with thin adapter fakes or shared handler harnesses.
- [ ] Exercise concurrent confirmation/reservation/release and rollback-on-error semantics with a suitable Convex test environment.
- [ ] Keep fast pure arithmetic tests and clearly separate fake-runtime checks from deployment-level concurrency guarantees.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Introduce intentional ownership/double-charge regression to confirm tests fail; run relevant API/session suites and documented runtime checks.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T08: Enforce ownership on private records and job controls](https://github.com/AustinKelsay/visibible/issues/64)
- [T11: Credit settled invoices first observed after expiry](https://github.com/AustinKelsay/visibible/issues/67)
- [T06: Finalize credits and durable cost reporting together](https://github.com/AustinKelsay/visibible/issues/62)

**Out of scope:**
- Image and bulk handler tests live with their implementation tickets; do not postpone those tests until this integration ticket.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S12: Verified migration and retirement of old implementations](https://github.com/AustinKelsay/visibible/issues/56). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 24, 69, 70.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
