> *This was generated by AI during triage.*

<!-- visibible-triage:2026-09-07:T47 -->

## Agent Brief

**Category:** enhancement
**State:** ready-for-agent
**Summary:** Apply bounded retention without losing financial evidence

**Current behavior:**
Cleanup exists for selected ephemeral tables but there is no program-wide bounded retention policy protecting all financial, unresolved and publication evidence.

**Desired behavior:**
Operators can dry-run cleanup and see exactly what can expire without removing balances or unresolved work.

**Key interfaces:**
- `publishToNostr`, publication identities/window claims and impression recording.
- Durable operation views, metric/health snapshots and bounded cleanup policies.

**Acceptance criteria:**
- [ ] Implement S11 retention classes with compact financial receipts and operation dedupe identities retained.
- [ ] Use bounded resumable cleanup for eligible diagnostics, processed outbox payloads, stale plans and minimized feedback context.
- [ ] Check linked state and blob references before deletion; unresolved/financial records are never swept by age alone.
- [ ] Provide dry-run counts, sampled identifiers and reconciliation totals with sensitive data redacted.
- [ ] New retention rules apply through a reviewed staged rollout; no destructive production cleanup is part of the implementation test.

**Verification:**
Source review confirms the existing behavior and the remaining target gap. This is a scoped improvement or verification task, not a claim of a newly reproduced production incident.

Synthetic linked data at expiry boundaries, interrupted cleanup and repeat run; verify retained balances, pending invoices, dedupe behavior and saved-image provenance.

Use actual application handlers with controlled external effects. Run repository-required checks and relevant regressions; for visible changes verify the affected browser flow. The isolated triage characterizations assert baseline defects and are not passing fix regressions. Update the affected current-behavior guide with implementation.

**Blocked by:**
- [T09: Preserve funded records when guest access expires](https://github.com/AustinKelsay/visibible/issues/65)
- [T06: Finalize credits and durable cost reporting together](https://github.com/AustinKelsay/visibible/issues/62)

**Out of scope:**
- No legal-compliance claim and no indefinite retention of avoidable raw diagnostic payloads.
- Sibling tickets, speculative platform replacement and production changes outside the applicable release procedure.

**Parent and policy:**
[S11: Trustworthy publication, operations and retention](https://github.com/AustinKelsay/visibible/issues/55). Its policy decisions remain part of this contract; implement only this ticket’s acceptance criteria.

**Redundancy and prior rejection:**
The current behavior above identifies existing capabilities to reuse and the gap still open. This ticket is not already implemented. No matching rejection knowledge-base entry was found.

**Traceability:** Original backlog items 80.

**Evidence baseline:** `6ae456839208ab6c97d630f80f8719becda9c050`. Recheck code and newer comments before implementation; source findings do not establish live incident frequency or provider spend.
